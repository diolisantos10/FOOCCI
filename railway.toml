[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npx prisma migrate resolve --rolled-back 20260518000001_add_distance_min_fee_km || true && npx prisma migrate deploy && npx next start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
