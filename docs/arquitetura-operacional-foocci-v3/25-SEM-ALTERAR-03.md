# Preservação do template 03

`foocci_contato_inicial_03` permanece `Olá! Tudo bem?` e não é renomeado. Se já existir na Meta, a rota idempotente retorna o registro existente em vez de tentar recriá-lo.
