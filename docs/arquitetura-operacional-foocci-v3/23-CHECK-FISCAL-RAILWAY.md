# Check de deployment

Nenhum commit desta branch deve ser descrito como deployed antes de merge na branch `claude/remove-legacy-runner-q8iXa` e confirmação explícita de um deployment Railway `SUCCESS` associado ao SHA de produção.
